{
  "name": "chess-local-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "tsc -w",
    "start": "python server.py",
    "test": "vitest --run",
    "ci": "npm run build && npm test"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.5.4",
    "vitest": "^1.0.0"
  }
}
